# ETF_manager 代码优化操作日志

**日期**: 2026-03-05  
**操作人**: AI Assistant  
**Git 分支**: main

---

## 概述

本次优化基于深度代码审计结果，按照 P0-P3 优先级顺序，分 6 个步骤逐步执行，每步完成后进行测试并提交 Git。

**共完成 6 次提交**，解决了以下核心问题：

| 优先级 | 问题 | 解决方案 |
|--------|------|----------|
| P0 | `init_db.py` DDL 与实际数据库不同步 | 重写 DDL 匹配实际 schema |
| P0 | `dashboard.py` 查询不存在的 `current_value` 列 | 动态计算 `shares * cost_price` |
| P0 | `tradable_shares` 计算错误 | 统一为 `shares - base_shares` |
| P1 | `export_excel.py` 引用不存在的 `current_value` | SQL 中动态计算估算市值 |
| P1 | 缺少 Service 层，业务逻辑耦合在 Router | 新建 `backend/services/` |
| P1 | Transaction 创建时不联动 Holdings | 在 service 层实现联动逻辑 |
| P2 | `quotes.py` 无分页，可能返回大量数据 | 添加分页参数 |
| P2 | `signals`/`rules` 更新时不检查记录是否存在 | 添加 404 校验 |
| P3 | 前端无统一错误处理 | 添加 axios 响应拦截器 |
| P3 | 前端存在重复代码 | 提取公共组件和工具函数 |

---

## Step 1: DDL 同步 + config.py + tradable_shares 修复

**提交**: `1a17d1a refactor: sync init_db DDL with actual schema, add config.py, fix tradable_shares, cleanup scripts`

### 1.1 新建 `config.py` — 集中配置管理

**路径**: `config.py`

消除项目中 6+ 处硬编码的 `DB_PATH`：

```python
import os

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
DATA_DIR = os.path.join(BASE_DIR, "data")
DB_PATH = os.path.join(DATA_DIR, "etf_manager.db")
OUTPUT_DIR = os.path.join(BASE_DIR, "output")
```

### 1.2 修复 `scripts/init_db.py` DDL

**问题**:
- `my_holdings` DDL 有 `current_value`, `latest_nav`, `nav_date` 但实际表有 `fund_name`
- `fund_info` DDL 缺少 `inception_date`

**修复**: 重写 DDL 完全匹配实际数据库 schema

### 1.3 修复 `tradable_shares` 计算逻辑

**路径**: `scripts/rebuild_holdings.py`

**问题**: `tradable_shares = shares` (100% 错误)

**修复**:
```python
base_shares = round(shares * 0.2, 2)
tradable_shares = round(shares - base_shares, 2)
```

### 1.4 清理废弃脚本

删除 7 个一次性迁移脚本：
- `migrate_data.py`
- `migrate_v2.py`
- `update_holdings.py`
- `update_gold_holdings.py`
- `update_fund_info_and_holdings.py`
- `update_fund_info_corrections.py`
- `insert_fund_002183.py`

### 1.5 新建 `.gitignore`

排除: `venv/`, `__pycache__/`, `node_modules/`, `data/*.db`, `data/*.png`, `output/`, `.codebuddy/`

---

## Step 2: Dashboard/export_excel current_value 动态计算

**提交**: `973ac19 fix(P1): dashboard/export_excel current_value -> shares*cost_price 动态计算`

### 2.1 修复 `backend/routers/dashboard.py`

**问题**: 查询 `SUM(current_value)` 但表中无此列，结果始终为 0

**修复**: 所有 4 个查询替换 `current_value` → `shares * cost_price`

```python
# 总资产查询
row = db.execute(
    "SELECT COALESCE(SUM(shares * cost_price), 0) as total_assets, "
    "COALESCE(SUM(total_invested), 0) as total_invested FROM my_holdings"
).fetchone()

# 分类分布
cat_rows = db.execute(
    "SELECT COALESCE(f.fund_category, '其他') as category, "
    "SUM(h.shares * h.cost_price) as value "
    "FROM my_holdings h LEFT JOIN fund_info f ON h.fund_code=f.fund_code "
    "GROUP BY f.fund_category ORDER BY value DESC"
).fetchall()

# 平台分布
plat_rows = db.execute(
    "SELECT platform, SUM(shares * cost_price) as value "
    "FROM my_holdings GROUP BY platform ORDER BY value DESC"
).fetchall()
```

### 2.2 修复 `scripts/export_excel.py`

**问题**: `SELECT h.current_value` 引用不存在的列

**修复**: SQL 中动态计算
```sql
SELECT h.platform, f.fund_name, f.fund_code, f.fund_company,
       f.fund_category, f.risk_level,
       h.shares, h.cost_price,
       ROUND(h.shares * h.cost_price, 2) AS estimated_value,
       h.base_shares, h.tradable_shares, h.total_invested,
       f.return_1y, f.return_3y
FROM my_holdings h
JOIN fund_info f ON h.fund_code = f.fund_code
```

### 2.3 验证 `backend/routers/holdings.py`

确认已正确处理：
- 用 `fi_fund_name` 别名解决 JOIN 产生的重复 `fund_name` 列
- 动态计算 `current_value`: 有最新净值用 `shares * latest_nav`，否则用 `shares * cost_price`

### 2.4 测试结果

```
测试1: Dashboard Summary 查询
  总资产(估算): ¥100,655.76
  总投入:       ¥100,956.96
  盈亏:         ¥-301.20
  收益率:       -0.30%
  ✓ total_assets > 0
  持仓基金数:   17
  ✓ fund_count == 17

测试2: 分类分布 - 分类汇总 = 总资产 ✓
测试3: 平台分布 - 平台汇总 = 总资产 ✓
测试4: Holdings 列表 - 所有 base_shares/tradable_shares 计算正确 ✓
测试5: export_excel SQL 验证 - 17条记录 ✓
```

---

## Step 3: Service 层 + Transaction→Holdings 联动

**提交**: `0d3ecd8 feat(P2): 添加 Service 层 + Transaction->Holdings 联动`

### 3.1 新建 `backend/services/` 目录

```
backend/services/
├── __init__.py
└── holding_service.py
```

### 3.2 `holding_service.py` 核心逻辑

```python
def apply_transaction(db, tx_type, fund_code, platform, amount, shares, nav_at_tx):
    """
    根据交易类型更新 my_holdings
    - 买入/定投: 增加份额、加权平均成本价、增加累计投入
    - 卖出: 减少份额、按比例减少累计投入
    """
    # 买入逻辑
    if tx_type in ("买入", "定投"):
        if holding:
            # 更新已有持仓 - 加权平均成本
            new_shares = old_shares + shares
            new_cost = (old_shares * old_cost + shares * nav_at_tx) / new_shares
            new_invested = old_invested + amount
        else:
            # 新建持仓
            # 从 fund_info 获取名称
    
    # 卖出逻辑
    elif tx_type == "卖出":
        new_shares = old_shares - shares
        # 按卖出比例减少累计投入
        sell_ratio = shares / old_shares
        new_invested = old_invested * (1 - sell_ratio)
```

### 3.3 更新 `transactions.py`

```python
from ..services.holding_service import apply_transaction

@router.post("")
def create_transaction(tx: TransactionCreate, db = Depends(get_db)):
    # 验证基金代码存在
    fund = db.execute("SELECT fund_code FROM fund_info WHERE fund_code=?", (tx.fund_code,)).fetchone()
    if not fund:
        raise HTTPException(status_code=400, detail=f"基金代码 {tx.fund_code} 不存在")
    
    # 插入交易记录
    cursor = db.execute("INSERT INTO transactions ...")
    
    # 联动更新 my_holdings
    if tx.tx_type in ("买入", "定投", "卖出") and tx.shares and tx.shares > 0:
        apply_transaction(db, tx.tx_type, tx.fund_code, tx.platform, 
                          tx.amount or 0, tx.shares, tx.nav_at_tx)
    
    db.commit()
    return {"tx_id": cursor.lastrowid, "message": "交易记录创建成功，持仓已更新"}
```

### 3.4 Holdings 新增端点

```python
@router.get("/{holding_id}")
def get_holding(holding_id: int, db = Depends(get_db)):
    """获取单个持仓详情"""
    
@router.delete("/{holding_id}")
def delete_holding(holding_id: int, db = Depends(get_db)):
    """删除持仓并重排 ID"""
```

### 3.5 测试结果 (8 项全部通过)

```
测试1: 买入 - 新建持仓 ✓
测试2: 买入 - 加仓(加权平均成本) ✓
测试3: 卖出 - 减仓 ✓
测试4: 卖出 - 全部卖出 ✓
测试5: 卖出不存在的持仓 - 静默跳过 ✓
测试6: 定投类型 ✓
测试7: 不同平台同基金是独立持仓 ✓
测试8: 验证真实数据库 import 能成功 ✓
```

---

## Step 4: quotes 分页 + signals/rules 存在性检查

**提交**: `53ce552 fix(P2): quotes 添加分页, signals/rules 添加存在性检查`

### 4.1 修复 `backend/routers/quotes.py`

**问题**: `fetchall()` 无分页，可能返回大量数据

**修复**:
```python
@router.get("")
def list_quotes(
    fund_code: str = None,
    date_from: str = None,
    date_to: str = None,
    page: int = Query(1, ge=1),
    page_size: int = Query(100, ge=1, le=1000),
    db = Depends(get_db),
):
    # 总记录数
    total = db.execute("SELECT COUNT(*) FROM daily_quotes q" + where, params).fetchone()[0]
    
    # 分页查询
    sql = "SELECT q.*, f.fund_name FROM daily_quotes q ... LIMIT ? OFFSET ?"
    rows = db.execute(sql, params + [page_size, offset]).fetchall()
    
    return {
        "total": total,
        "page": page,
        "page_size": page_size,
        "data": [dict(r) for r in rows],
    }
```

### 4.2 修复 `backend/routers/signals.py`

**问题**: `PATCH /{signal_id}` 不检查信号是否存在

**修复**:
```python
@router.patch("/{signal_id}")
def update_signal_status(signal_id: int, update: SignalStatusUpdate, db = Depends(get_db)):
    row = db.execute("SELECT signal_id FROM trade_signals WHERE signal_id=?", (signal_id,)).fetchone()
    if not row:
        raise HTTPException(status_code=404, detail=f"信号 {signal_id} 不存在")
    # ... 更新逻辑
```

### 4.3 修复 `backend/routers/rules.py`

**问题**: `PUT /{rule_id}` 和 `PATCH /{rule_id}/toggle` 不检查规则是否存在

**修复**: 同上，添加存在性检查

### 4.4 前端适配

**`frontend/src/views/ChartsView.vue`**:
```typescript
const loadQuotes = async () => {
  const params: any = { page_size: 1000 }
  // ...
  const res = await getQuotes(params)
  quotes.value = (res.data.data || []).reverse()  // 适配新格式
}
```

**`frontend/src/api/index.ts`**:
```typescript
export const getQuotes = (params?: {
  fund_code?: string
  date_from?: string
  date_to?: string
  page?: number
  page_size?: number  // 新增
}) => api.get('/quotes', { params })
```

---

## Step 5: 前端 axios 错误拦截器 + 公共组件提取

**提交**: `7bc0e5e feat(P3): axios 错误拦截器, 提取 StatCard/echarts/format 公共模块`

### 5.1 添加 axios 响应拦截器

**路径**: `frontend/src/api/index.ts`

```typescript
import { MessagePlugin } from 'tdesign-vue-next'

const api = axios.create({
  baseURL: '/api',
  timeout: 10000,
})

// 响应拦截器：统一错误处理
api.interceptors.response.use(
  (response) => response,
  (error) => {
    const msg =
      error.response?.data?.detail ||
      error.response?.data?.message ||
      error.message ||
      '请求失败'
    MessagePlugin.error(msg)
    return Promise.reject(error)
  }
)
```

### 5.2 提取 `utils/format.ts`

```typescript
/** 格式化数字，保留指定小数位 */
export const formatNum = (n: number | null | undefined, decimals = 2): string =>
  (n ?? 0).toFixed(decimals)

/** 格式化为人民币金额 */
export const formatCurrency = (n: number | null | undefined): string =>
  '¥' + formatNum(n)
```

### 5.3 提取 `utils/echarts.ts`

统一 ECharts 注册，消除各页面重复的 `use([...])` 调用：

```typescript
import VChart from 'vue-echarts'
import { use } from 'echarts/core'
import { PieChart, BarChart, LineChart } from 'echarts/charts'
import { TitleComponent, TooltipComponent, LegendComponent, GridComponent, DataZoomComponent } from 'echarts/components'
import { CanvasRenderer } from 'echarts/renderers'

use([PieChart, BarChart, LineChart, TitleComponent, TooltipComponent, 
     LegendComponent, GridComponent, DataZoomComponent, CanvasRenderer])

export default VChart
```

### 5.4 提取 `components/StatCard.vue`

```vue
<template>
  <t-card class="text-center" :bordered="true">
    <div class="text-sm text-gray-500 mb-1">{{ label }}</div>
    <div class="text-2xl font-bold" :class="valueClass">
      <slot>{{ value }}</slot>
    </div>
    <slot name="extra"></slot>
  </t-card>
</template>

<script setup lang="ts">
defineProps<{
  label: string
  value?: string | number
  valueClass?: string
}>()
</script>
```

### 5.5 配置 `@` alias

**`frontend/vite.config.ts`**:
```typescript
import path from 'path'

export default defineConfig({
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src'),
    },
  },
})
```

**`frontend/tsconfig.app.json`**:
```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
```

### 5.6 重构 DashboardView

**之前** (106 行):
```vue
<t-card class="text-center" :bordered="true">
  <div class="text-sm text-gray-500 mb-1">总资产</div>
  <div class="text-2xl font-bold text-blue-700">¥{{ formatNum(summary.total_assets) }}</div>
</t-card>
<!-- 重复 3 次 -->
```

**之后** (精简约 20%):
```vue
<StatCard label="总资产" :value="'¥' + formatNum(summary.total_assets)" value-class="text-blue-700" />
<StatCard label="总投入" :value="'¥' + formatNum(summary.total_invested)" value-class="text-gray-700" />
<StatCard label="总收益" :value-class="...">
  {{ summary.total_pnl >= 0 ? '+' : '' }}¥{{ formatNum(summary.total_pnl) }}
</StatCard>
<StatCard label="持仓基金数" :value="summary.fund_count">
  <template #extra>
    <div v-if="summary.pending_signals > 0">...</div>
  </template>
</StatCard>
```

---

## Step 6: 清理 data/ 目录

**结果**: 目录已干净，无需操作

**当前状态**:
```
data/
├── 持仓数据快照_20260305.md   # 项目数据快照 (git 跟踪)
├── 交易规则.md                 # 参考文档 (git 跟踪)
└── etf_manager.db              # 数据库文件 (gitignore 排除)
```

`.gitignore` 已配置排除: `data/*.db`, `data/*.pptx`, `data/*.pdf`, `data/*.docx`, `data/*.png`

---

## 文件变更汇总

### 后端新增
- `config.py` — 集中配置管理
- `backend/services/__init__.py` — Service 层初始化
- `backend/services/holding_service.py` — 持仓业务逻辑

### 后端修改
- `backend/database.py` — 使用 `config.DB_PATH`
- `backend/routers/dashboard.py` — 动态计算市值
- `backend/routers/holdings.py` — 新增 GET/DELETE 端点
- `backend/routers/transactions.py` — 联动 Holdings
- `backend/routers/quotes.py` — 添加分页
- `backend/routers/signals.py` — 添加存在性检查
- `backend/routers/rules.py` — 添加存在性检查
- `scripts/init_db.py` — DDL 同步
- `scripts/rebuild_holdings.py` — tradable_shares 修复
- `scripts/export_excel.py` — 动态计算市值
- `scripts/verify_db.py` — 使用 config
- `scripts/init_fund_info.py` — 使用 config

### 后端删除 (7 个废弃脚本)
- `scripts/migrate_data.py`
- `scripts/migrate_v2.py`
- `scripts/update_holdings.py`
- `scripts/update_gold_holdings.py`
- `scripts/update_fund_info_and_holdings.py`
- `scripts/update_fund_info_corrections.py`
- `scripts/insert_fund_002183.py`

### 前端新增
- `frontend/src/utils/format.ts` — 格式化工具
- `frontend/src/utils/echarts.ts` — ECharts 统一注册
- `frontend/src/components/StatCard.vue` — 统计卡片组件

### 前端修改
- `frontend/src/api/index.ts` — axios 拦截器 + quotes 参数
- `frontend/src/views/DashboardView.vue` — 使用新组件和工具
- `frontend/src/views/ChartsView.vue` — 使用新工具 + 适配分页
- `frontend/vite.config.ts` — 添加 @ alias
- `frontend/tsconfig.app.json` — 添加 paths

### 根目录新增
- `.gitignore` — Git 忽略配置

---

## Git 提交历史

```
7bc0e5e feat(P3): axios 错误拦截器, 提取 StatCard/echarts/format 公共模块, 配置 @ alias
53ce552 fix(P2): quotes 添加分页, signals/rules 添加存在性检查, 前端适配 quotes 分页返回格式
0d3ecd8 feat(P2): 添加 Service 层 + Transaction->Holdings 联动, holdings 增加 GET/{id} DELETE/{id} 端点
973ac19 fix(P1): dashboard/export_excel current_value -> shares*cost_price 动态计算, holdings.py 已正确处理 duplicate fund_name 和动态 current_value
7a2db23 fix: dashboard/holdings dynamically compute current_value from shares*cost_price or latest NAV
1a17d1a refactor: sync init_db DDL with actual schema, add config.py, fix tradable_shares, cleanup scripts
716c3db Initial commit: ETF Manager project with backend, frontend, scripts and data
```

---

## 遗留问题 (后续迭代可处理)

1. **前端组件进一步提取**: `FundSelect`, `PlatformSelect`, `DataTable`, `FormDialog` 等
2. **Transaction 添加 UPDATE/DELETE 端点**: 当前只有 GET 和 POST
3. **`data/交易规则.md` 中的 DDL 描述过时**: 需更新文档
4. **前端 vue-tsc 编译问题**: tsconfig 配置需进一步调整
5. **Service 层扩展**: 可将 DCA、Signals 等业务逻辑也迁移到 service 层

---

**日志结束**
